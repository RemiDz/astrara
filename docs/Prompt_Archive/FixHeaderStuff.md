Three header fixes: 1) Replace the ℹ️ info button emoji with a simple text character — use a lowercase 'i' in a subtle circle, styled as: className='w-5 h-5 rounded-full border border-white/20 text-white/30 text-\[10px] flex items-center justify-center hover:text-white/50 hover:border-white/30 transition-colors select-none' — no emoji, just clean minimal text. 2) The info button is pushing the ASTRARA title up and misaligning the header. Move the info button to sit on the same line as 'Live Cosmic Intelligence' subtitle, right after the text, not next to the main title. Or alternatively place it at the far right of the subtitle line. Make sure the ASTRARA title stays vertically aligned as before. 3) The location text in the header jumps from left to its final position on page load. This is because 'Detecting location...' text is a different width than the final city name. Fix: give the location container a fixed minimum width (min-w-\[100px]) and right-align the text so the position doesn't shift when the city name loads in.

