Add EN/LT language toggle to both /about and /promo pages. Use the same i18n system and language state that the main Astrara app uses. If the main app stores language preference in localStorage, read from the same key so the language stays consistent across all pages. Add a small language toggle button in the top-right corner of both pages — same styling as the main app's settings. All static content on both pages needs Lithuanian translations. For the /promo page, when Lithuanian is selected, the Claude API prompts should include an instruction to generate the horoscope reading in Lithuanian. Add to the system prompt: 'Write the entire reading in Lithuanian language.' when the language is set to LT. Run npm run build, commit: feat: EN/LT language support for /about and /promo pages, push to main.

