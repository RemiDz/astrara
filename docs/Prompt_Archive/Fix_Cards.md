Two CSS fixes for planet cards. 1) Make card backgrounds much more transparent — change the background to rgba(255,255,255,0.03) or bg-white/\[0.03] so cards almost disappear into the dark background and text feels like floating in space. Remove any solid background colours on cards. 2) Fix the top border/line on cards — it extends to the card edges but the card has rounded corners, so the line overshoots. The border must follow the card's border-radius. If the top line is a separate element or pseudo-element, apply the same border-radius to it, or use overflow: hidden on the card container, or replace the top line with a border-top on the card itself which automatically follows the rounding. Apply both fixes to ALL planet cards, moon phase card, and cosmic weather cards. Run npm run build, commit: fix: transparent card backgrounds and border-radius on top line, push to main.

